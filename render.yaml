services:
  - type: web
    name: kadastro360-web-pilot
    runtime: node
    plan: free
    buildCommand: npm ci --omit=dev && npm run check
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 22.16.0
      - key: TEST_USERNAME
        value: admin
      - key: TEST_PASSWORD
        sync: false
      - key: SESSION_SECRET
        generateValue: true
      - key: ADMIN_PANEL_PIN
        sync: false
      - key: DEFAULT_DAILY_QUOTA
        value: 20
      - key: DATA_DIR
        value: /tmp/kadastro360-data
      - key: ROUTING_BASE_URLS
        value: https://router.project-osrm.org,https://routing.openstreetmap.de/routed-car
