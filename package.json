{
  "name": "kadastro360-web-pilot",
  "version": "1.5.0",
  "private": true,
  "description": "Kadastro360 canlı veri web pilotu; görsel içeriği doğrulanan ULASAV/TUCBS WMS katmanları ve güvenilir plan ölçeği denetimi",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check account-store.js && node --check terrain.js && node --check endeksa-utils.js && node --check open-data.js && node test-pilot.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "pngjs": "^7.0.0"
  }
}
